// productList.forEach(function (productList, index, copyOfProdcutList) {});

// forEach;
// Map;

const productList = [
  {
    productName: "Banana",
    price: 250,
  },
  {
    productName: "orange",
    price: 400,
  },
];



 const finalValues = movieList.forEach(function (copyOfMovieList, index) {
    return copyOfMovieList + " " + index;
  });

  console.log(finalValues, "finalValues");


console.log(result)


// function forEach(copyOfProductList, index) {
//   for (let index = 0; index < copyOfProductList.length; index++) {
//     const element = copyOfProductList[index];
//     return element + ' ' + index;
//   }
// }
